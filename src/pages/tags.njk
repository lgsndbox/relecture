---
layout: layouts/base.njk
title: Tous les tags
description: Parcourez les articles de PAUSE par mots-clés
permalink: /tags/
breadcrumbs:
  - text: Accueil
    url: /
  - text: Tags
---

<div class="tags-page">
    <header class="page-header">
        <h1 class="page-title">Tous les tags</h1>
        <p class="page-description">Explorez les articles de PAUSE à travers leurs mots-clés</p>
    </header>

    <div class="tags-cloud">
        {% set allTags = collections.articles | getAllTags %}
        {% if allTags.length > 0 %}
            {% for tag in allTags %}
            <a href="/tags/{{ tag.slug }}/" class="tag tag--cloud" data-count="{{ tag.count }}">
                <svg aria-hidden="true" width="1em" height="1em" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20.59 13.41l-7.17 7.17a2 2 0 0 1-2.83 0L2 12V2h10l8.59 8.59a2 2 0 0 1 0 2.82z"></path>
                    <line x1="7" y1="7" x2="7.01" y2="7"></line>
                </svg>
                <span class="tag-name">{{ tag.name }}</span>
                <span class="tag-count" aria-hidden="true">{{ tag.count }}</span>
                <span class="sr-only">{{ tag.count }} article{% if tag.count > 1 %}s{% endif %}</span>
            </a>
            {% endfor %}
        {% else %}
            <p class="no-tags">Aucun tag disponible pour le moment.</p>
        {% endif %}
    </div>

    <div class="tags-list">
        <h2>Liste alphabétique</h2>
        {% set alphabeticalTags = allTags | sort(false, false, 'name') %}
        <ul class="tags-alphabetical">
            {% for tag in alphabeticalTags %}
            <li class="tag-list-item">
                <a href="/tags/{{ tag.slug }}/" class="tag-list-link">
                    <span class="tag-list-name">{{ tag.name }}</span>
                    <span class="tag-list-count">{{ tag.count }} article{% if tag.count > 1 %}s{% endif %}</span>
                </a>
            </li>
            {% endfor %}
        </ul>
    </div>
</div>
