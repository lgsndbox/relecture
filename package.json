{
  "name": "eleventytest",
  "version": "1.0.0",
  "description": "PAUSE - La revue qui prend son temps",
  "main": "index.js",
  "scripts": {
    "check:duplicates": "node scripts/check-duplicates.js",
    "validate:references": "node scripts/validate-references.js",
    "validate": "npm run validate:references && npm run check:duplicates",
    "standardize:frontmatter": "node scripts/standardize-frontmatter.js",
    "bundle:css": "node scripts/bundle-css.js",
    "hash:js": "node scripts/hash-js.js",
    "bundle:assets": "npm run bundle:css && npm run hash:js",
    "generate:favicons": "node scripts/generate-favicons.js",
    "build:dev": "ELEVENTY_ENV=development npm run validate && eleventy",
    "build:prod": "ELEVENTY_ENV=production npm run validate && eleventy",
    "build": "npm run build:prod",
    "postbuild": "npm run search:index",
    "search:normalize": "node scripts/normalize-for-search.js",
    "search:pagefind": "pagefind --site _site_normalized",
    "search:copy": "node -e \"const fs = require('fs'); const path = require('path'); const src = '_site_normalized/pagefind'; const dest = '_site/pagefind'; fs.cpSync(src, dest, {recursive: true});\"",
    "search:cleanup": "node -e \"const fs = require('fs'); fs.rmSync('_site_normalized', {recursive: true, force: true});\"",
    "search:index": "npm run search:normalize && npm run search:pagefind && npm run search:copy && npm run search:cleanup",
    "start": "ELEVENTY_ENV=development eleventy --serve",
    "serve": "npm start",
    "serve-prod": "ELEVENTY_ENV=production eleventy --serve",
    "test": "npm run validate"
  },
  "keywords": [
    "eleventy",
    "blog",
    "french",
    "magazine"
  ],
  "author": "Équipe PAUSE",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-img": "^6.0.4",
    "@11ty/eleventy-plugin-rss": "^2.0.4",
    "@quasibit/eleventy-plugin-sitemap": "^2.2.0",
    "lightningcss": "^1.30.2",
    "pagefind": "^1.4.0",
    "esbuild": "^0.27.2",
    "glob": "^13.0.0"
  },
  "devDependencies": {
  }
}
